# Bias Detection (Phase 4)

Survivorship bias and look-ahead bias detection, built with generic inputs (timestamped data + outcome labels) so this module is shared with the CFIP project rather than hardcoded to one data domain.

Every function here should be testable against synthetic examples with a known correct answer (see NFR-5.2).
