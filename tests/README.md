# Tests

Pytest test suite. Every core function in src/ should have at least one test here (NFR-5.1).

Mirror the src/ folder structure: tests/ingestion/, tests/validation/, etc., as modules are added.
