# n8n Workflows

Exported n8n workflow JSON files. Scoped strictly to orchestration and scheduling (triggering ingestion pipelines, sending alerts) per FR-1.7 — core research/validation/bias-detection logic lives in src/, not inside n8n nodes.
